-- a script that computes the score average of all records in the table second_table of the database hbtn_0c_0 in MySQL server.
-- The result column name is set to average
SELECT AVG(`score`) AS average FROM `second_table`;
